<h1> Make Your Appearances </h1>


<%= form_for @appearance do |f| %>
<%= f.label :rating %>
<%= f.number_field(:rating, in: 1..5 ) %>
<%= f.select :episode_id,Episode.all, :id, :date %>
<%= f.select :guest_id,Guest.all, :id, :name %>
<%= f.submit %>

<% end %>
